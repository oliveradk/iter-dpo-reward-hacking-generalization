openai
inspect-ai
python-dotenv
# >=4: older releases can't read datasets saved with the `List` feature type
# (oliverdk/nl_gameable_programmatic_graders, oliverdk/impossible_apps_intro)
datasets>=4.0
numpy
pandas
matplotlib
seaborn
scipy
tqdm
tyro
chess
gradio
jinja2
tinker>=0.22.3
tinker-cookbook[inspect]>=0.4.1
together[cli]
wandb
modal
s3fs
awscrt  # botocore[crt] — needed for the `aws login` credential provider
zstandard
# decisiveness_eval (capabilities_evals/decisiveness.py); the Case V fit needs
# torch, which arrives transitively via tinker-cookbook
question-decisiveness @ git+https://github.com/ArcadiaImpact/question-decisiveness.git
# ifeval_eval / gsm8k_eval (capabilities_evals/{ifeval,gsm8k}.py) wrap the
# upstream inspect_evals tasks; the IFEval scorer additionally needs the
# reference instruction checker (inspect_evals's `ifeval` optional dep) +
# langdetect
inspect-evals
instruction_following_eval @ git+https://github.com/josejg/instruction_following_eval
langdetect
# terminal_bench_eval (capabilities_evals/terminal_bench.py) — inspect bridge to
# Harbor-packaged benchmarks (terminal-bench). Requires Python >= 3.12.
inspect-harbor
# swe_bench_eval (capabilities_evals/swe_bench.py) — the upstream scorer parses
# test output with swebench's log parsers (inspect_evals's `swe_bench` optional dep)
swebench
# verifiers RL environments (prime-rl integration — specs/verifiers_integration.md)
verifiers
