services:
  default:
    image: codecontests-rh-env:latest
    build:
      context: .
      dockerfile: Dockerfile
    command: ["tail", "-f", "/dev/null"]
    working_dir: /workspace
    # Network isolation (optional; comment out for tests that need net)
    # network_mode: none
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 2G
